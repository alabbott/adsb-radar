[reticulum]
  enable_transport = False
  share_instance   = No

[interfaces]

  # Connect directly to the transport container (Docker DNS resolves "transport").
  [[TCP to transport]]
    type              = TCPClientInterface
    interface_enabled = True
    target_host       = transport
    target_port       = 7777
